# markets-o11y Kubernetes Secrets - Production
# Copy to .env.prod and fill in values
# NEVER commit .env.prod to git!

# === Database ===
POSTGRES_USER=markets
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=markets

# === Grafana ===
GF_SECURITY_ADMIN_PASSWORD=change-me-in-production
