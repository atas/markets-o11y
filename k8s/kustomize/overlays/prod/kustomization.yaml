apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: markets-o11y-prod

resources:
  - ../../base

labels:
  - pairs:
      app.kubernetes.io/env: prod
    includeSelectors: false

# Secrets are managed manually - see k8s/manual/prod/ for templates
# Run: make apply-secrets-prod (from k8s/ directory)

# Image customization
images:
  - name: ghcr.io/atas/markets-o11y-worker
    newTag: sha-e24fa15
